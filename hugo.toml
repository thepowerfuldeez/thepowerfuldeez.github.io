baseURL = 'https://ggrigorev.me/'
languageCode = 'en-us'
title = 'George Grigorev Blog'
theme = 'paper'

[params]
  # Used by the Paper theme for the header
  name = 'George Grigorev'
  bio = 'Previously at Together AI (distributed training + evals) and Snap (multimodal LLMs used by 100M+ people). I work across text, image, and speech; now focused on training efficiency, sample efficiency, and small coding LLMs.'
  mainSections = ['posts']
  color = 'linen'

[menu]
  [[menu.main]]
    identifier = 'about'
    name = 'About'
    url = '/'
    weight = 10
  [[menu.main]]
    identifier = 'publications'
    name = 'Publications'
    url = '/publications/'
    weight = 20
  [[menu.main]]
    identifier = 'blog'
    name = 'Blog'
    url = '/posts/'
    weight = 30

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[caches]
  [caches.images]
    dir = ':cacheDir/images'
